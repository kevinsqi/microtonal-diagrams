export type DiagramConfig = {
  count: number;
  circleColor: string;
  lineColor: string;
  radius: number;
  separation: number;
  textBelowCircle: string[];
  textInCircle: string[];
  textColor: string;
};
